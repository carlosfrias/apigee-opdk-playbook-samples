---
- include: configuration/opdk-credentials.yml
  vars:
    hosts: 'planet'

- include: components/opdk-upload-install-apigee-mirror.yml
  vars:
    hosts: 'planet'
    jdk_version: '1.8'
  tags:
  - upload_install

